# ADR-0004: Enable LIVE trading — K60 H100 TP3%/SL1.5% T0.57, $10

Date: 2026-09-07. Status: accepted (user-directed).

## Decision
Flip `alpha3-dry-runner` from testnet paper (`demo-fapi.binance.com`) to
LIVE USDT-M futures (`fapi.binance.com`) on unchanged params:
K=60, H=100, TP 3%, SL 1.5%, threshold 0.57, 10 assets (+ZECUSDT),
stake 20%, leverage 20x, $10 funding.

## Context
- Model `models/meta_labeler.joblib` sha256 `1d61e860619c149b`
  (K60 H100 TP0.03 SL-0.015, thr 0.57, OOF AUC 0.575, commits `dc0caf7`+`0bd6fbb`).
- In-sample T0.57: prec 0.362, sel 10.6% (`models/threshold_sweep_results.json`);
  breakeven 34.4% at 2:1 RR + 0.05% fee. OOF ~4pp lower (~0.32) — below
  breakeven. Walk-forward family verdict remains NO-GO (0/108, AGENTS.md 1.10);
  K/H OOF grid (`scripts/search_kh_tp3_sl15.py`) incomplete at flip time.
- First T0.57 paper fill: BULLAUSDT LONG → TIMEOUT +1.38% (+$0.55), equity $10.55, 1/1.
- Pre-flip live account: flat (all 10 positions 0.0), keys valid, balance $0.00
  at check time — funding ($10) in progress. Restart gated on balance > 0.

## Changes
- `.env`: `BINANCE_USE_TESTNET=false`, `BINANCE_USE_LIVE=true`, `TRADING_MODE=live`.
- `systemd/alpha3-dry-runner.service`: `Environment=TRADING_MODE=live
  BINANCE_USE_LIVE=true BINANCE_USE_TESTNET=false`; description LIVE.
- K/H/TP/SL/threshold/stake/leverage: unchanged (user: USE CURRENT PARAMS).
- Backup: `dry_data/live_preflip_20260907_150241/` (state $10.55, 1W/0L).

## Consequences / risks
- 20x on likely-negative-EV model: full-loss of $10 possible in a few trades.
  Circuit breaker 3L→50-bar cooldown, kill flag
  `dry_data/alpha3_kill.flag`, `FLATTEN_ON_SHUTDOWN` remain armed.
- Entries place live + demo + testnet legs in parallel (pre-existing pattern).
- Rollback: `.env` back to testnet, `daemon-reload`, restart (paper resumes,
  state preserved via migrate path).

## Alternatives considered
- Staged: finish K/H grid + 50-trade testnet milestone first (recommended by
  agent, rejected by user — immediate live flip with $10 cap chosen).
- Reduced size (5% × 5x) for first live run (rejected — current params kept).
