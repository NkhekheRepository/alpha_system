# Alpha Trading System — Dependencies
# Python >= 3.14 required (multiprocessing forkserver default)

# Core numerical
numpy>=2.0.0
pandas>=3.0.0

# Binance API / live spot polling (60s)
requests>=2.28.0

# Feather I/O
pyarrow>=17.0.0

# Statistics (Kelly MC, Sharpe, permutation)
scipy>=1.14.0

# QuantConnect LEAN framework (required by alpha_1percent.py)
quantconnect-stubs>=18000

# CCXT (exchange connectivity — not currently used in live runners, kept for future)
ccxt>=4.0.0

# ML / calibration (optional, used by alpha_1percent.py skeleton)
scikit-learn>=1.5.0

# Logging (optional)
loguru>=0.7.0

# Telegram bot (alerts from live runners)
python-telegram-bot>=22.0
python-dotenv>=1.0.0

# Charts (reporting)
matplotlib>=3.8.0
