# Alpha Trading System — Python Dependencies
# Target: Python >= 3.11 (tested on 3.14). Lower 3.11+ works for live runners.
# Install: pip install -r requirements.txt

# Core numerical
numpy>=1.24.0
pandas>=2.0.0

# HTTP / exchange connectivity (price feed + demo-fapi hedge orders)
requests>=2.28.0

# Model persistence (frozen meta-labeler: models/meta_labeler.joblib)
joblib>=1.3.0

# Machine learning (meta-labeler training + feature transforms)
scikit-learn>=1.3.0

# Statistics (Sharpe, drawdown, Kelly MC, permutation tests)
scipy>=1.11.0

# Charts / reporting (equity curves, trade charts)
matplotlib>=3.7.0

# Feather I/O (backtest feather datasets)
pyarrow>=14.0.0

# Telegram alerts (live trade notifications, command bot)
python-telegram-bot>=20.0

# .env configuration loading
python-dotenv>=1.0.0
